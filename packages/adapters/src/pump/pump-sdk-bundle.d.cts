// Hand-written declaration for the esbuild-generated pump-sdk-bundle.cjs
// (gitignored, regenerated on every build — see package.json's
// build:pump-sdk-bundle:* scripts and pump-sdk-entry.mjs). Its actual
// runtime exports are exactly @pump-fun/pump-sdk's own exports, re-cast
// to that real type at each import site rather than duplicated here.
declare const pumpSdkBundle: unknown;
export = pumpSdkBundle;
